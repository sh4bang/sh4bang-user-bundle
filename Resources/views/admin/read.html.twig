{% extends 'base-admin.html.twig' %}

{% trans_default_domain 'sh4bang_user' %}

{% block body %}
    {% if users|length %}
        <ul>
        {% for user in users %}
            <li>{{ user.email }}</li>
        {% endfor %}
        </ul>
    {% endif %}
{% endblock %}
